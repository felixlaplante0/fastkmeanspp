version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.13"

submodules:
  recursive: true

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .

sphinx:
  configuration: docs/source/conf.py
